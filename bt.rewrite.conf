# 适用于宝塔“伪静态”面板里 location 规则不生效的场景。
# 敏感文件保护请优先使用 nginx.rewrite.conf 里的 location 规则，或放到站点“配置文件”server 块内。
rewrite ^/?$ /index.php last;
rewrite ^/(geoflow-agent/.*)$ /index.php/$1 last;
rewrite ^/(article/.*)$ /index.php/$1 last;